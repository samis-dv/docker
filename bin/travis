#!/usr/bin/env bash

set -e

push=true
version=${TRAVIS_TAG}
if [ "${version}" == "" ]; then
  push=false
  version=${TRAVIS_BRANCH}
fi

if [ $push == true ]; then
  build --type all --version ${version} --push
else
  build --type all --version ${version}
fi
