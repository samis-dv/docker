#!/usr/bin/env bash

set -e

root=$(cd $(dirname ${0})/../; pwd)
source ${root}/bin/source/argsf

namespace=$(argument namespace directus)
prefix=$(argument prefix "")

${root}/bin/build --image ${namespace}/${prefix}directus --version develop

