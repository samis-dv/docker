#!/usr/bin/env bash

set -e

#
# Common
#

source $(dirname $0)/common

#
# Fetch a release
#

echo "Downloading '${REPOSITORY_FULLNAME}' release named '${RELEASE_NAME}'"

curl -L https://github.com/${REPOSITORY_FULLNAME}/archive/${RELEASE_NAME}.zip -o release.zip --ssl-no-revoke
mkdir -p ./releases/
unzip -q -u -d ./releases/ release.zip
rm -f release.zip
